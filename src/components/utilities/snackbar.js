export const handlesnapbar = (severity, message, setSnackbarSeverity, setSnackbarMessage, setSnackbarOpen) => {
    setSnackbarSeverity(severity);
    setSnackbarMessage(message);
    setSnackbarOpen(true);
  };
  